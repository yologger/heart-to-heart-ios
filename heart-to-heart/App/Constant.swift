enum Constant {
    
    enum API {
        static let AuthBaseUrl = "http://localhost:8000"
    }
    
    enum Key {
        static let ThemeKey = "USER_DEFAULT_THEME_KEY"
        static let SessionKey = "USER_DEFAULT_SESSION_KEY"
    }
}
